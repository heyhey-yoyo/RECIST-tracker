{
  "name": "recist-tracker",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-only RECIST 1.1 / iRECIST assessment assistant for Cloudflare Pages.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm run test && npm run build",
    "preview": "python3 -m http.server 4173 -d dist"
  },
  "engines": {
    "node": ">=20"
  }
}
